$('.slider-principal').slick({
    dots: true, //botões
    infinite: true, //animação infinita
    speed: 300, //velocidade
    slidesToShow: 1,//quantidade de slides por vez
    adaptiveHeight: true, //adaptar as alturas
    autoplay: true, //autoplay
    autoplaySpeed: 2000 //velocidades autoplay
});